"""a0 package: pure constants, enums, type aliases, config dataclasses.

Tier contract (from `.ass-ade/tier-map.json` -> `_tiers.a0`):
no logic, no I/O, no side effects, no upward imports into a1..a4.
"""
