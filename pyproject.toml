[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ass_ade_rebuild"
version = "0.1.1"
description = "Auto-emitted rebuild package"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["."]
