FROM python:3.12-slim

WORKDIR /app

# Copy only what the bot needs — keeps image small
COPY pyproject.toml README.md ./
COPY src/ ./src/
COPY scripts/atomadic_discord_bot.py ./scripts/

RUN pip install --no-cache-dir -e ".[discord]"

CMD ["python", "scripts/atomadic_discord_bot.py"]
