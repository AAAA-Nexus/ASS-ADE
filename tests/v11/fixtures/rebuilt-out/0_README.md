# minimal_pkg — Monadic Rebuild

Auto-generated by ASS-ADE rebuild engine · rebuild `20260422_232614` · 2026-04-22

This folder is a tier-partitioned monadic rebuild of `C:\!atomadic\ass-ade-v1.1\tests\fixtures\minimal_pkg`, produced
by the Atomadic ecosystem rebuilder (AAAA-SPEC-003). Every source symbol has
been classified, restructured, and validated against public structural invariants.

## Tier structure

| Tier | Components | Purpose |
|------|-----------|---------|
| `a1_at_functions` | 4 | Pure functions (atoms) |
| `a2_mo_composites` | 1 | Stateful compositions (molecules) |

**Total components:** 5

## Structural status

- Conformant: YES
- Pass rate: 40.0%
- CNA compatibility: compatible (0 finding(s) across 10 checked file(s))
- Generated tests: 2 artifact(s) (100% baseline coverage)
- Documentation coverage: 100% effective across 8 public symbol(s)
- Multi-language bridges: `typescript`, `rust`, `kotlin`, `swift` (manifest only)
- Certificate: `CERTIFICATE.json` (AAAA-SPEC-006/CERT-1)
- Output recon report: `RECON_REPORT.md`

## Quick start

```bash
# Explore by tier
ls a0_qk_constants/        # constants and axioms
ls a1_at_functions/        # pure atoms
ls a2_mo_composites/       # stateful engines
ls a3_og_features/         # feature organisms
ls a4_sy_orchestration/    # orchestration

# Verify the certificate
python -c "import json,hashlib; c=json.load(open('CERTIFICATE.json')); h=c.pop('certificate_sha256'); b=json.dumps(c,sort_keys=True).encode(); print('OK' if hashlib.sha256(b).hexdigest()==h else 'TAMPERED')"

# Run the generated baseline verification suite
python -m pytest tests/ -q
```
