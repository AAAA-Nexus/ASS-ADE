name: ass-ade-ship
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Ship audit
        run: echo "Ship audit placeholder"
