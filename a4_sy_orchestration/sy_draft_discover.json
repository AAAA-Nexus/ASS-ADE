{
  "body": "def discover(self, capability_embedding: Any, k: int = 5) -> list[ToolRef]:\n    self._calls += 1\n    if self._nexus is not None and hasattr(self._nexus, \"discovery_search\"):\n        try:\n            cap = capability_embedding if isinstance(capability_embedding, str) else str(capability_embedding)\n            result = self._nexus.discovery_search(cap, limit=k)\n            tools: list[ToolRef] = []\n            items = getattr(result, \"results\", None) or getattr(result, \"tools\", None) or []\n            for idx, item in enumerate(items[:k]):\n                name = getattr(item, \"name\", None) or (item.get(\"name\") if isinstance(item, dict) else f\"tool_{idx}\")\n                score = getattr(item, \"score\", None) or (item.get(\"score\") if isinstance(item, dict) else 1.0 - idx * 0.1)\n                server = getattr(item, \"server\", \"\") or (item.get(\"server\", \"\") if isinstance(item, dict) else \"\")\n                tools.append(ToolRef(name=name, score=float(score), server=server))\n            if tools:\n                return tools\n        except Exception:\n            pass\n    return self._catalog[:k]\n",
  "body_hash": "29787597232c2a57",
  "body_truncated": false,
  "callers_of": [
    "hasattr",
    "discovery_search",
    "enumerate",
    "isinstance",
    "str",
    "getattr",
    "append",
    "ToolRef",
    "get",
    "float"
  ],
  "component_schema": "ASSADE-SPEC-003",
  "description": "Draft candidate from C:/!ass-ade-evoMERGE-g3-20260419-003649/a4_sy_orchestration/sy_draft_mcpzerorouter.py:17.",
  "exceptions_raised": [],
  "fulfills_blueprints": [],
  "id": "sy.source.a4_sy_orchestration.discover",
  "imports": [
    "__future__.annotations"
  ],
  "interfaces": {
    "source": "C:/!ass-ade-evoMERGE-g3-20260419-003649/a4_sy_orchestration/sy_draft_mcpzerorouter.py:17"
  },
  "kind": "ecosystem_system",
  "made_of": [
    "og.source.a3_og_features.get"
  ],
  "name": "discover",
  "product_categories": [
    "COR"
  ],
  "provides": [
    "rebuild candidate for discover"
  ],
  "reuse_policy": "reference-only",
  "status": "draft",
  "tier": "a4_sy_orchestration",
  "version": "0.1.0"
}
