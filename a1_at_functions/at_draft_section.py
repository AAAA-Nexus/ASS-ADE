# Extracted from C:/!ass-ade/.claude/worktrees/beautiful-dubinsky-c2cb48/a1_at_functions/at_draft_run_all.py:7
# Component id: at.source.ass_ade.section
__version__ = "0.1.0"

    def section(title: str) -> None:
        if only and only.lower() not in title.lower():
            return
        print(f"{CYAN}{'-' * 70}{RESET}")
        print(f"{CYAN}  {title}{RESET}")
        print(f"{CYAN}{'-' * 70}{RESET}")
