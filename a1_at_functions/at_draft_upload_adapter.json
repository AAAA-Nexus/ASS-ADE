{
  "body": "def upload_adapter(cfg: TrainConfig, adapter_dir: Path) -> tuple[str, str]:\n    \"\"\"Push the adapter artifact to the configured backend.\n\n    Returns (adapter_url, weights_sha256).\n    \"\"\"\n    # Compute a content hash over the adapter_model.safetensors for integrity.\n    weights_path = adapter_dir / \"adapter_model.safetensors\"\n    if not weights_path.exists():\n        # older peft versions use .bin\n        weights_path = adapter_dir / \"adapter_model.bin\"\n    if not weights_path.exists():\n        raise RuntimeError(f\"no adapter weights found under {adapter_dir}\")\n    sha = hashlib.sha256(weights_path.read_bytes()).hexdigest()\n\n    if cfg.upload_backend == \"hf\":\n        return _upload_hf(cfg, adapter_dir, sha), sha\n    if cfg.upload_backend == \"r2\":\n        return _upload_r2(cfg, adapter_dir, sha), sha\n    # local: just point at the on-disk path (useful for dev / GH Actions\n    # artifact handoff; the Worker obviously can't fetch file:// but a\n    # subsequent manual upload can promote the same URL).\n    return f\"file://{adapter_dir.resolve()}\", sha\n",
  "body_hash": "a5c70f7cb158c35a",
  "body_truncated": false,
  "callers_of": [
    "hexdigest",
    "exists",
    "RuntimeError",
    "sha256",
    "_upload_hf",
    "_upload_r2",
    "read_bytes",
    "resolve"
  ],
  "component_schema": "ASSADE-SPEC-003",
  "description": "Draft candidate from C:/!ass-ade/.claude/worktrees/adoring-boyd-0e3a8f/scripts/lora_train.py:217.",
  "exceptions_raised": [
    "RuntimeError"
  ],
  "fulfills_blueprints": [],
  "id": "at.source.ass_ade.upload_adapter",
  "imports": [
    "__future__.annotations",
    "argparse",
    "hashlib",
    "json",
    "logging",
    "os",
    "sys",
    "time",
    "dataclasses.asdict",
    "dataclasses.dataclass",
    "pathlib.Path",
    "typing.Any",
    "httpx",
    "subprocess",
    "torch",
    "datasets.Dataset",
    "peft.LoraConfig",
    "peft.TaskType",
    "peft.get_peft_model",
    "transformers.AutoModelForCausalLM",
    "transformers.AutoTokenizer",
    "transformers.DataCollatorForLanguageModeling",
    "transformers.Trainer",
    "transformers.TrainingArguments",
    "huggingface_hub.HfApi",
    "huggingface_hub.login"
  ],
  "interfaces": {
    "source": "C:/!ass-ade/.claude/worktrees/adoring-boyd-0e3a8f/scripts/lora_train.py:217"
  },
  "kind": "pure_function",
  "made_of": [],
  "name": "upload_adapter",
  "product_categories": [
    "COR"
  ],
  "provides": [
    "rebuild candidate for upload_adapter"
  ],
  "reuse_policy": "reference-only",
  "status": "draft",
  "tier": "a1_at_functions",
  "version": "0.1.0"
}
