[build]
builder = "NIXPACKS"
buildCommand = "pip install -e '.[discord]'"

[deploy]
startCommand = "python scripts/atomadic_discord_bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
