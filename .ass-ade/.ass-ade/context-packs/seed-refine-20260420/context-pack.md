# Context pack (seed — REFINE cycle closed PASS)

**Intent:** Demonstrate a **real** `context-pack.json` beside code with `verificationEvidence` on each research hop (MAP = TERRAIN).

**Repo map:** See `repoMap` in sibling JSON; ASS-ADE monadic tree + `mcp/server.json` authoritative.

**Web research:** Three hops (H1 MCP scale, H2 verification literature, H3 synthesis) — evidence pointers are **paths/tests**, not vibes.

**Tech docs:** Stack fingerprint from repo manifests only; deliverable scopes empty for this seed.

**Next:** Copy this folder pattern per feature; replace summaries with task-specific content; keep `verificationEvidence` filled or explicit `DEFER`.
